import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(255, 45, 32, 1);
const secondaryColor = Color.fromRGBO(31, 6, 4, 1.0);
const bgColor = Colors.black;

const defaultPadding = 16.0;
